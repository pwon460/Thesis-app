<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="simo.transport"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="20" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="simo.transport.activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="simo.transport.activities.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name="simo.transport.activities.SelectTripActivity"
            android:label="@string/title_activity_select_trip" >
        </activity>
        <activity
            android:name="simo.transport.activities.BusOptionsActivity"
            android:label="@string/title_activity_select_route_type" >
        </activity>
        <activity
            android:name="simo.transport.activities.TripActivity" >
        </activity>
        <activity
            android:name="simo.transport.activities.ShowTripActivity"
            android:label="@string/title_activity_show_trip" >
        </activity>
    </application>

</manifest>