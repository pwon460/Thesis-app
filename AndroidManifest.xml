<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="simo.transport"
    android:screenOrientation="portrait"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="20" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name=".activities.SelectTransportActivity"
            android:label="@string/title_activity_select_transport" >
        </activity>
        <activity
            android:name=".activities.BusOptionsActivity"
            android:label="@string/title_activity_select_route_type" >
        </activity>
        <activity android:name=".activities.SelectTripActivity" />
        <activity android:name=".activities.SelectBusTripActivity" />
        <activity
            android:name=".activities.ShowTimetableActivity"
            android:label="@string/title_activity_timetable" >
        </activity>
        <activity
            android:name="simo.transport.activities.ViewTripActivity"
            android:label="@string/title_activity_view_trip" >
        </activity>
    </application>

</manifest>